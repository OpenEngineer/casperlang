import "std"

printbool b::Bool = echo (show b) + "\n"

printboolalt b::Bool = 
  if b (echo "true\n") (echo "false\n")

main =
  x = (1==1 && 2==2) == False;
  printbool x;
  printboolalt x;
  echo (showf (toFloat "0.4123") )+ "\n"
